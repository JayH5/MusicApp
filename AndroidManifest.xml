<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="za.jamie.soundstage"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="16"
        android:targetSdkVersion="17" />

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.BROADCAST_STICKY" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.GET_TASKS" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/icn_first_attempt"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="za.jamie.soundstage.activities.LibraryActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver android:name="za.jamie.soundstage.service.MusicIntentReceiver" >
            <intent-filter>
                <action android:name="android.media.AUDIO_BECOMING_NOISY" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.MEDIA_BUTTON" />
            </intent-filter>
        </receiver>

        <service
            android:name="za.jamie.soundstage.service.MusicService"
            android:label="@string/app_name"
            android:process=":musicservice" >
            <intent-filter>
                <action android:name="za.jamie.soundstage.action.TOGGLE_PLAYBACK" />
                <action android:name="za.jamie.soundstage.action.PLAY" />
                <action android:name="za.jamie.soundstage.action.PAUSE" />
                <action android:name="za.jamie.soundstage.action.NEXT" />
                <action android:name="za.jamie.soundstage.action.PREVIOUS" />
                <action android:name="za.jamie.soundstage.action.STOP" />
            </intent-filter>
        </service>
        
         <service
            android:name="za.jamie.soundstage.bitmapfun.ImageDownloadService"
            android:label="@string/app_name" />

        <activity
            android:name="za.jamie.soundstage.activities.AlbumBrowserActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait" >
        </activity>
        <activity
            android:name="za.jamie.soundstage.activities.ArtistBrowserActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait" >
        </activity>
    </application>

</manifest>